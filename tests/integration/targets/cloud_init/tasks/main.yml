---
# Integration tests for vergeio.vergeos.cloud_init module
# Note: Cloud-init file management requires specific SDK capabilities
# that may not be available in all environments

- name: "cloud_init: Skip test - cloud_init requires specific SDK support"
  ansible.builtin.debug:
    msg: "cloud_init tests skipped - SDK cloud-init file management not available in current pyvergeos version"
