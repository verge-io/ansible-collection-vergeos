---
namespace: vergeio
name: vergeos
version: 2.1.0
readme: README.md
authors:
  - VergeIO
description: Ansible collection for managing VergeOS virtualization infrastructure
license:
  - GPL-3.0-or-later
license_file: ""
tags:
  - vergeio
  - vergeos
  - virtualization
  - infrastructure
  - cloud
# These three ship inside MANIFEST.json and are what Galaxy renders as the
# Repository / Documentation / Issue Tracker links on the collection page.
# They pointed at verge-io/ansible-vergeos, which 404s; the repository is
# verge-io/ansible-collection-vergeos. Wrong metadata was harmless while
# unpublished and is not once it is published -- it shipped in the v2.1.0
# artifact. tests/unit/test_galaxy_metadata.py keeps them honest.
repository: https://github.com/verge-io/ansible-collection-vergeos
documentation: https://github.com/verge-io/ansible-collection-vergeos/blob/main/README.md
homepage: https://verge.io
issues: https://github.com/verge-io/ansible-collection-vergeos/issues
build_ignore:
  - .git
  - .gitignore
  - .DS_Store
  - .claude
  - .mcp.json
  - CLAUDE.md
  - "*.tar.gz"
  - __pycache__
  - "*.pyc"
  - .venv
  # fnmatch against the path relative to the collection root: no path is ever
  # equal to "tests/", so the trailing slash made this entry match nothing and
  # the whole test tree shipped in every artifact (57 entries on 2.0.1). The
  # neighbouring entries work because none of them has one. See issue #15.
  - tests
  # ansible-galaxy does NOT read .gitignore, so anything gitignored but absent
  # from build_ignore is published whenever the artifact is built from a
  # working tree rather than a clean clone.
  - .probe
  - .pytest_cache
  - .github
  - .ansible-lint
