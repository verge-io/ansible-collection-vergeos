# The floor is higher than main's >=1.0.1 because this branch adds modules
# that need managers the older SDK does not have. Measured by installing each
# version and introspecting VergeClient against the 24 managers our modules
# reach for:
#
#   1.0.1  missing: physical_drives      (breaks physical_drive_info, drive_health)
#   1.1.0  missing: physical_drives
#   1.2.0  missing: physical_drives
#   1.2.3  missing: none                 <- hard minimum for manager availability
#   1.2.5  adds the OData escaping fix (pyvergeos#72) that any .get(name=...)
#          with an apostrophe in the name depends on
#   1.2.7  what the 887 unit tests and 24 live ladders are actually verified
#          against
#
# Declaring 1.2.7 rather than the 1.2.3 hard minimum: claiming support for a
# version nothing has been run on is not support. Lower it if someone runs the
# suite against 1.2.3/1.2.5 and it passes.
#
# Note this is NOT required for the update-persistence or drive-tier fixes any
# more -- those write API field names directly and need no SDK cooperation.
# Verified against BOTH released 1.2.7 and origin/dev (22 commits ahead) on
# 2026-09-22: 15 live ladders, identical task counts, failed=0 on both, plus
# 929 unit tests. See docs/SDK-COMPATIBILITY.md.
#
# Do not raise this floor until pyvergeos#96/#97/#98 are RELEASED -- they are
# fixed on dev and unreleased -- and do not swap client-side name matching for
# list(name=...) when it moves, because #100 is still open and survives #96.
pyvergeos>=1.2.7
#
# physical_drive_info node: uses PhysicalDriveManager(node_key=...) only
# when the installed SDK has the pyVergeOS#143 parent_drive walk
# (_parent_drive_filter_for_node). Released builds from 1.2.7 through
# 1.6.1 still filter "node eq <key>" against a column machine_drive_phys
# does not have, and that list is always empty. On those SDKs the module
# matches node_name client-side. Do not raise this floor to pick up #143;
# it is unreleased.
