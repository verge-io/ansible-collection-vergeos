# Floor.
#
# Manager availability is satisfied at 1.2.3 (physical_drives exists).
# 1.2.5 adds the OData apostrophe escape (pyvergeos#72). 1.2.7 was the
# previous verified floor.
#
# 1.2.8 is the brace fix: quote_value() escapes '{' (pyVergeOS#100,
# PR #114). That version was prepared (pyVergeOS#122) and not published
# to PyPI. The next published release is 1.6.1, which contains the fix,
# so `>=1.2.8` installs 1.6.1 from PyPI. Verified through 1.6.1 on
# 2026-09-23. See docs/SDK-COMPATIBILITY.md.
#
# Client-side name matching stays. #100 is no longer the reason: the
# floor includes the fix. Ambiguous names still are (#72), and switching
# lookups to list(name=...) is a behavior change, not part of this floor.
pyvergeos>=1.2.8
#
# physical_drive_info node: scoping needs the PhysicalDriveManager(
# node_key=...) fix from pyVergeOS#143 / PR #144. That landed on the
# SDK's `dev` branch; until it is cut into a release newer than the
# 1.6.1 that still lacked it, lab and CI that exercise `node:` must
# install that tip (editable checkout of origin/dev) rather than PyPI
# 1.6.1. Unscoped scans work on 1.2.7+ either way.
